---
description: cool-admin-vue 反模式约束（防止把后台页面写复杂）
globs: *.vue, *.ts, *.tsx
---

# cool-admin-vue 反模式规则

本规则专门用于限制后台页面开发时的“跑偏写法”。

## 核心原则

**如果这是一个普通后台页面，就不要把它写成自定义前端框架练习题。**

cool-admin 已经提供了：

- 页面组织方式
- 模块组织方式
- CRUD 交互模式
- 搜索与高级搜索模式
- 表格展示模式
- 新增/编辑弹窗模式
- 服务调用组织方式

因此默认应该：

- 优先复用
- 优先贴近 demo
- 优先贴近官方文档
- 优先降低复杂度

## 常见错误写法

### 错误 1：自己重新封请求

反例特征：

- 在页面中直接写大量请求逻辑
- 新建一套独立的接口层，与项目 service 并存
- 普通 CRUD 页自己维护获取列表、删除、详情、保存的全部调用

正确方式：

- 优先复用项目中已有 `service`
- 优先沿用 demo 中 service 接入方式
- 只有当现有 service 能力明显不够时，才补最小新增能力

### 错误 2：自己重新封页面状态机

反例特征：

- 手写 `dialogVisible`
- 手写 `formData`
- 手写 `query`
- 手写 `pagination`
- 手写 `tableData`
- 手写 `submitLoading`
- 手写整套 reset / open / edit / save / search / reload

正确方式：

- 优先 `useCrud`
- 优先 `useTable`
- 优先 `useUpsert`
- 优先 `useSearch`
- 优先 `useAdvSearch`
- 优先 `cl-crud`、`cl-table`、`cl-upsert` 等组件组合

### 错误 3：普通页面过度 composable 化

反例特征：

- 为一个普通后台页面拆出多个自定义 composable
- 页面本身逻辑不复杂，却把搜索、表格、表单、弹窗分别封成一套自定义 hooks
- 最后项目里同时存在 cool 体系和自定义体系两套模式

正确方式：

- 普通页面优先贴近 demo
- 通用能力复用 cool 提供的 hooks
- 只有跨多个页面稳定复用的复杂能力，才考虑额外抽象

### 错误 4：无视 demo 的目录与写法

反例特征：

- 文件位置和模块结构明显偏离 `src/modules/demo`
- 新页面没有参考同类 demo
- 页面命名、组件命名、模块接入方式混乱

正确方式：

- 先找 demo 中同类页面
- 优先复用相同目录结构
- 优先保持命名和接入方式一致

## 标准页面默认选型

### 标准 CRUD 页面

默认优先：

- `cl-crud`
- `cl-table`
- `cl-upsert`
- `cl-search`
- `cl-pagination`
- `useCrud`
- `useTable`
- `useUpsert`
- `useSearch`

### 高级筛选页面

在标准 CRUD 基础上补：

- `cl-adv-search`
- `useAdvSearch`

### 单纯展示型后台页面

可以简化使用：

- `cl-table`
- `cl-pagination`
- 少量 `el-*` 组件

但仍然优先复用 service 与现有模块结构。

## 什么时候可以不用这些封装

只有下面情况才允许明显偏离：

- 页面不是 CRUD 型后台页面
- 交互模型明显超出 demo 范围
- cool 现有封装无法满足目标
- 当前项目已有稳定且更高优先级的既有实现模式

即便如此，也要先说明：

1. 为什么不能直接用 cool 官方范式
2. 哪些部分仍然沿用 cool 能力
3. 为什么新的复杂度是必要的

## 编码前快速判断

开始写之前，先问自己：

- 这是不是后台管理中的标准页面？
- demo 里有没有相似例子？
- 官方文档里有没有现成模式？
- 我是不是准备手写一整套 cool 已经做过的东西？

只要答案偏向“是”，就应该回到 cool 官方范式。

## 最终目标

让基于这个 base 开发出来的后台代码具备这些特征：

- 统一
- 可预测
- 接近官方范式
- 容易维护
- 不因 AI 或个人习惯而无序膨胀

如果一个普通后台页面写完后让人感觉“怎么这么复杂”，优先怀疑实现方式偏离了本规则。

---
description: cool-admin-vue 开发总控规则（优先官方范式与 demo）
globs: *.vue, *.ts, *.tsx
---

# cool-admin-vue 开发总控规则

本规则用于约束基于 `cool-admin-vue` 的后台开发方式。

目标只有一个：**优先走 cool-admin 官方文档与 demo 已经提供的开发范式，不要把普通后台页面写成一套自定义、复杂、分散的通用 Vue 应用。**

## 最高优先级参考顺序

处理后台页面、CRUD、表单、搜索、表格、模块扩展时，按下面顺序参考：

1. 当前项目中同类实现
2. `src/modules/demo` 中最接近的 demo 写法
3. `https://vue.cool-admin.com/` 官方文档
4. 当前目录下其他 `.cursor/rules/*.mdc`
5. 通用 Vue 最佳实践

如果第 1～3 项已经能覆盖，不要退回到“自己设计一套页面状态流和请求流”。

## 开发默认路径

### 先判断是不是标准后台页面

只要需求属于以下任意一种，默认按 cool-admin 官方后台模式开发：

- 列表页
- 增删改查页面
- 带搜索/筛选的管理页
- 弹窗新增/编辑页
- 表格联动页
- 字典、状态、枚举展示页
- 常规后台运营配置页

这类页面默认优先：

- `cl-crud`
- `cl-table`
- `cl-search`
- `cl-adv-search`
- `cl-upsert`
- `cl-form`
- `useCrud`
- `useTable`
- `useUpsert`
- `useSearch`
- `useAdvSearch`
- `useCool()` 中已有能力
- 项目内已注册好的 `service`

### 再判断是不是插件/模块扩展

如果是模块扩展，优先遵循：

- `src/modules/<module-name>` 放业务模块
- `src/plugins/<plugin-name>` 放可复用插件能力
- 页面、组件、hooks、store、config.ts 按模块结构组织

## 强制要求

### 1. 优先参考 demo

写新页面前，先找 `src/modules/demo` 中最接近的示例。

尤其优先参考：

- `views/crud` 相关 demo
- `cl-crud` 组合示例
- `cl-form` / `cl-upsert` / `cl-search` / `cl-table` 示例

如果 demo 已经有现成模式，就以 demo 的组织方式为主，而不是重造。

### 2. 优先复用 cool 的服务与交互体系

后台标准页面不要自己重新设计下面这些基础能力：

- 请求调用方式
- 列表分页流转
- 搜索参数管理
- 弹窗开关状态
- 新增/编辑提交流程
- 表格列渲染基础能力
- 字典映射与基础状态展示

如果 cool 已经封装过，就必须优先复用。

### 3. 页面实现要尽量“像 cool-admin”

产出的代码应该满足：

- 一眼能看出是 cool-admin 项目
- 目录结构与 demo 基本同路
- CRUD、搜索、表单、表格关系清楚
- 页面状态收敛，不四处分散
- 业务逻辑与通用交互逻辑分层清晰

### 4. 常规后台需求，复杂度必须受控

对于普通后台管理需求：

- 能用 `cl-crud` 解决的，不要拆成多套自定义表格+弹窗+表单流
- 能用 `useCrud`/`useTable`/`useUpsert` 组织的，不要手写大量重复状态
- 能通过现有 `service` 调用的，不要重造业务请求层

目标是：**简单问题保持简单，标准问题走标准路径。**

## 明确禁止的反模式

以下写法默认视为反模式，除非任务明确要求或现有项目已经这样做：

### 1. 在常规后台 CRUD 页面中手写第二套请求体系

例如：

- 页面里直接大段 `axios` / `fetch`
- 单独再封一套与 cool 冲突的 `api.ts` / `request.ts`
- 本来能用项目 `service`，却重新封装接口调用层

### 2. 在页面里手写完整的列表/搜索/分页/弹窗/提交状态流

如果只是标准后台管理页，不要自己维护：

- `loading`
- `dialogVisible`
- `formData`
- `queryParams`
- `page/pageSize/total`
- `submitLoading`
- `selectedRows`

这一整套重复状态，除非 cool 现有能力确实覆盖不了。

### 3. 把普通后台页面写成通用互联网前端架构

不要在普通后台场景里：

- 过度抽象 composable
- 过度拆分状态管理
- 过度引入通用 UI 架构模式
- 把一个简单 CRUD 页做成复杂业务中台前端结构

### 4. 绕开 demo 和官方文档自由发挥

如果官方文档和 demo 已经有推荐做法，就不要凭习惯写成另一种体系。

## 允许例外的情况

以下情况可以不完全按标准 CRUD/dem​​o 模式：

- 高度定制化交互页面
- 可视化画布类页面
- 强实时交互页面
- 复杂富文本/大文件/特殊编辑器页面
- 第三方复杂组件深度集成页面
- demo 明显无法覆盖的复杂业务流程

但即使是例外，也要：

- 先说明为什么不能沿用 cool 默认模式
- 尽量局部突破，不要全局推翻
- 仍优先复用现有 service、模块结构、命名和注册方式

## 输出检查清单

写完页面前自检：

1. 我是否先参考了 `src/modules/demo`？
2. 这个页面是否本来可以用 `cl-crud` / `cl-upsert` / `cl-search` 实现？
3. 我是否重复造了请求封装或页面状态流？
4. 这份代码看起来是否像 cool-admin 项目，而不是普通 Vue 项目？
5. 如果用了更复杂写法，是否有明确理由？

如果 2、3、4 任一答案不理想，就优先回退到 cool 官方范式。

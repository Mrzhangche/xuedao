# 数据库设计文档

## 适用场景

用于新增表、修改表结构、增加字段、调整字段语义、增加索引、增加状态字段、设计数据迁移方案时。

## 基本信息

- 业务名称：
- 所属模块：
- 目标端：
- 关联需求：

## 设计目标

说明这次数据库调整是为了解决什么问题。

## 表设计

### 表名

- 表名：
- 用途：
- 说明：

### 字段设计

| 字段名 | 类型 | 必填 | 默认值 | 索引 | 说明 |
| --- | --- | --- | --- | --- | --- |
| [字段] | [类型] | [是/否] | [默认值] | [是否] | [说明] |

### 状态字段说明

如果存在状态字段，必须写清楚每个值的含义。

| 字段 | 值 | 含义 |
| --- | --- | --- |
| [status] | [0] | [禁用] |

## 索引设计

| 索引名 | 字段 | 类型 | 作用 |
| --- | --- | --- | --- |
| [索引名] | [字段] | [普通/唯一] | [作用] |

## 与其他表的关系

- 关联表：
- 关联字段：
- 关系说明：

注意：这里只写业务关系说明，不代表一定要在 Entity 中使用外键关系装饰器。

## 数据读写规则

- 谁会写入这张表
- 谁会读取这张表
- 哪些字段由系统维护
- 哪些字段前端可传

## 数据迁移或兼容说明

- 是否需要历史数据兼容
- 是否需要初始化数据
- 是否需要脚本迁移

## 风险与注意事项

- [风险 1]
- [注意 1]

## 简短示例

新增 `coupon_info` 表，用于保存优惠券基础信息。`status` 字段含义为：`0-禁用`、`1-启用`、`2-已过期`。`couponCode` 需要唯一索引，避免重复生成。
